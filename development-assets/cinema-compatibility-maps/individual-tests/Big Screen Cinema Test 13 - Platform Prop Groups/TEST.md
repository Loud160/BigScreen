# Test 13: Custom-platform preference and merged cloned-light groups

Expected behavior:

- allowCustomPlatform=false is published while video is active
- The cloned light remains functional with mergePropGroups=true
- Quest needs a platform-mod API consumer to verify platform suppression

The difficulty intentionally contains no notes, bombs, walls, arcs,
chains, or gameplay objects. Lighting pulses are present to make bloom
and transparency behavior observable.
