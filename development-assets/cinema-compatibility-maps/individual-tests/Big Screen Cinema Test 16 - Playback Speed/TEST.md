# Test 16: Mapper playbackSpeed

Expected behavior:

- Video plays at exactly half speed (playbackSpeed=.5)
- Video clock advances one second per two audio pulses
- Screen remains responsive when paused, resumed or scrubbed

The difficulty intentionally contains no notes, bombs, walls, arcs,
chains, or gameplay objects. Lighting pulses are present to make bloom
and transparency behavior observable.
